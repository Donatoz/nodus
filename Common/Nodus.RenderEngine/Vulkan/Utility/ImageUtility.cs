using Silk.NET.Vulkan;

namespace Nodus.RenderEngine.Vulkan.Utility;

public static class ImageUtility
{
    public static Format GetSupportedFormat(Format[] candidateFormats, PhysicalDevice physicalDevice, ImageTiling tiling, FormatFeatureFlags features)
    {
        return default;
    }
}